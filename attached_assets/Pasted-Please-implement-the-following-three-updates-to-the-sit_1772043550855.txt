Please implement the following three updates to the site. They are independent but should be applied together.

1. Update Insights Page – Per‑Article PDF Links
File: src/pages/Insights.tsx

Remove the global PDF link currently at the bottom of the page (the one with “📄 Download Technical Bulletin (PDF)”).

For each of the two mock articles, add a dedicated PDF link directly below the article’s excerpt.

Use the following link text for both articles:
📄 View Technical Insight (PDF)

Set the href attribute to the placeholder PDF file names:

First article (15 February 2026): href="/pdfs/Technical_Insight_001.pdf"

Second article (10 January 2026): href="/pdfs/Technical_Insight_002.pdf"

Style the link to match the site’s design (e.g., text-primary hover:underline inline-flex items-center gap-1).

Keep all other content (headings, dates, titles, excerpts) exactly as they are.

Example structure for each article:

tsx
<div>
  <p className="text-sm text-primary/60 mb-2">{item.date}</p>
  <h2 className="text-2xl font-display font-semibold mb-2">{item.title}</h2>
  <p className="text-muted-foreground mb-4">{item.excerpt}</p>
  <a href="/pdfs/Technical_Insight_001.pdf" className="text-primary hover:underline inline-flex items-center gap-1">
    📄 View Technical Insight (PDF)
  </a>
</div>
2. Modify Contact Form to Work with Netlify Forms
File: src/pages/Home.tsx – this is the “Begin Your Voyage” section.

Update the <form> element:

Remove any existing onSubmit handler (if present).

Add these attributes:
data-netlify="true", name="contact", netlify-honeypot="bot-field", method="POST".

Inside the form, add the required hidden fields (preferably at the top):

html
<input type="hidden" name="form-name" value="contact" />
<input type="hidden" name="bot-field" />
Hide the honeypot field using a CSS class (e.g., className="hidden").

Add a JavaScript submission handler. You can define it inside the component:

tsx
const handleSubmit = async (e: React.FormEvent<HTMLFormElement>) => {
  e.preventDefault();
  const form = e.currentTarget;
  const formData = new FormData(form);
  try {
    const response = await fetch("/", {
      method: "POST",
      headers: { "Content-Type": "application/x-www-form-urlencoded" },
      body: new URLSearchParams(formData as any).toString(),
    });
    if (response.ok) {
      alert("Thank you! Your message has been sent.");
      form.reset();
    } else {
      alert("Oops! Something went wrong. Please try again.");
    }
  } catch (error) {
    alert("Network error. Please check your connection.");
  }
};
Attach this handler to the form: onSubmit={handleSubmit}.

Keep all other form fields (email input, message textarea, submit button) unchanged.

3. Update Navigation Links for Smooth Page Scrolling
File: src/components/Navigation.tsx (if the navigation is in a separate component; otherwise locate the relevant code in Home.tsx or wherever the main header lives).

Goal: Certain buttons should scroll to specific sections on the home page.

3.1. Identify the target sections in Home.tsx
The “Begin Your Voyage” section (contains the contact form) – if it doesn’t have an id, add one:
<section id="begin-voyage">

The “Core Competencies” section (overview of services) – if it doesn’t have an id, add one:
<section id="core-competencies">

3.2. Update the relevant buttons/links
Button / Link	Current Target	New Target (href or click handler)
“Request a quote” button (hero section)	(unknown)	#begin-voyage
“Inquire” button (navigation)	(unknown)	#begin-voyage
“Explore Services” button (hero section)	(unknown)	#core-competencies
If the navigation uses <a> tags, simply set href="#begin-voyage" and href="#core-competencies".

If the navigation uses buttons with click handlers, implement a smooth scroll function:

tsx
const scrollToSection = (id: string) => {
  const element = document.getElementById(id);
  if (element) {
    element.scrollIntoView({ behavior: 'smooth' });
  }
};
Then attach it, e.g.:

tsx
<button onClick={() => scrollToSection('begin-voyage')}>Inquire</button>
Ensure that the target sections exist and that the IDs are correctly spelled.

Please apply all three changes and confirm when done. If any file paths differ from the ones mentioned, adjust accordingly. Thank you!