1. Fix Contact link navigation
Change the Contact link in the navigation menu so that it always goes to the homepage footer.
Update href from #contact to /#contact in the navigation component.
If already on homepage, make it scroll smoothly to the footer.
Also update the same link in the footer menu if present.

2. Please update the Core Competencies section in src/pages/Home.tsx as follows:
For each of the six boxes, remove the paragraph text (the long descriptive sentence) and keep only the bullet list below the heading. The bullet lists should be exactly as shown below. Do not add or change anything else.

3. Adjust Navigation Links for Smooth Page Scrolling
There are three buttons that need to scroll to specific sections on the home page:

The "Request a quote" button (located in the hero section at the top of the page).
The "Inquire" button (located in the navigation menu).
The "Explore Services" button (also in the hero section).

First, make sure the target sections have IDs that we can link to.
In src/pages/Home.tsx, find the section titled "Begin Your Voyage" (this is where the contact form is). If it doesn't already have an id, add one to its container element. The ID should be something like begin-voyage.

Find the section that lists your main services (likely called "Core Competencies" or similar). If it doesn't have an id, add one. The ID should be something like core-competencies.

Now, modify the buttons so that when clicked, the page scrolls smoothly to the corresponding section.
For the "Request a quote" button: make it scroll to the begin-voyage section.
For the "Inquire" button (in the navigation): also scroll to the begin-voyage section.
For the "Explore Services" button: make it scroll to the core-competencies section.

How you implement this depends on what the buttons currently are:
If they are <a> tags, you can simply set their href attribute to #begin-voyage or #core-competencies.

If they are <button> elements with click handlers, replace the click handler with one that uses document.getElementById(...).scrollIntoView({ behavior: 'smooth' }).

Make sure the scrolling is smooth (use the behavior: 'smooth' option).
Do not change any other styling or behavior of these buttons.
